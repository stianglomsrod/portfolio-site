---
import profil from '../data/profil.json';

const aar = new Date().getFullYear();
---

<footer class="bunn">
  <div class="indre">
    <p class="kolofonlinje">
      © {aar} Stian Glomsrød · bygget i åpen dialog med Claude Code · ingen
      cookies, ingen sporing
    </p>
    <p class="lenker">
      <a href="/kolofon">kolofon</a>
      <span aria-hidden="true">·</span>
      <a href="/kolofon#tilgjengelighet">tilgjengelighet</a>
      <span aria-hidden="true">·</span>
      <a href="/kolofon#personvern">personvern</a>
      <span aria-hidden="true">·</span>
      <a href={`mailto:${profil.kontakt.epost}`}>e-post</a>
      <span aria-hidden="true">·</span>
      <a href={profil.kontakt.github} rel="me">GitHub</a>
      <span aria-hidden="true">·</span>
      <a href={profil.kontakt.linkedin} rel="me">LinkedIn</a>
    </p>
  </div>
</footer>

<style>
  .bunn {
    border-top: 1px dashed var(--linje);
    margin-top: var(--s-8);
  }

  .indre {
    max-width: 1060px;
    margin: 0 auto;
    padding: var(--s-4) var(--s-6) var(--s-5);
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: var(--s-3);
    flex-wrap: wrap;
    font-family: var(--font-mono);
    font-size: 11px;
  }

  .kolofonlinje {
    margin: 0;
    color: var(--dempet);
  }

  .lenker {
    margin: 0;
    color: var(--dempet);
    display: flex;
    gap: var(--s-2);
    flex-wrap: wrap;
  }
</style>
